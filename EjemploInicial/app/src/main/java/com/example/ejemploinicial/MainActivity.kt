package com.example.ejemploinicial

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main)

        //var miBoton = findViewById(R.id.btnAceptar)
        //miBoton.setOnClickListener(){
            //var miCaja:TextView = findViewById(R.id:txtCaja)
            //miCaja.text = "Cambiado desde codigo"
            //Toast.makeText(this,"Texto al pulsar,Toast.LENGTH_LONG").show()
        //}
    }
}